spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: root
    password: root
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true